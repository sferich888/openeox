{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://localhost/schemas/product.json",
  "title": "Product definition outlining known Versions and Components",
  "description": "Defines the structure for a Product object, including its versions and associated components.",
  "type": "object",
  "definitions": {
    "schema_t": {
      "title": "OpenEoX Lifecycle schema",
      "description": "Specifies the schema the JSON object must be valid against.",
      "type": "string",
      "enum": [
	"http://localhost/schemas/product-lifecycle.json"
      ]
    },
    "Product": {
      "type": "object",
      "description": "Represents a product with its name, vendor, versions, and components.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The unique name of the product."
        },
        "vendor_name": {
          "type": "string",
          "description": "The name of the vendor for this product."
        },
        "versions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Version"
          },
          "description": "List of versions for this product."
        },
        "components": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Component"
          },
          "description": "List of components associated with this product."
        }
      },
      "required": [
        "name",
        "vendor_name",
        "versions"
      ]
    },
    "Version": {
      "type": "object",
      "description": "Represents a specific version of a product, referencing lifecycle phases from the main schema.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name or identifier of the version."
        },
        "lifecycle_phases": {
          "type": "array",
          "items": {
            "$ref": "product-lifecycle.json#/definitions/LifecyclePhase"
          }
        }
      },
      "required": [
        "name",
        "lifecycle_phases"
      ]
    },
    "Component": {
      "type": "object",
      "description": "Represents a component, an included product or dependency that included/associated with a product.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The unique name of the component within the product."
        },
        "product_ref": {
          "type": "string",
          "description": "References the product name or definition that defines this component as a product."
        }
      },
      "required": [
        "name",
        "product_ref"
      ]
    }
  },
  "$ref": "#/definitions/Product"
}
