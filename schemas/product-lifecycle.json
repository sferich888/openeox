{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://localhost/schemas/product-lifecycle.json",
  "title": "Lifecycle Object Specification",
  "description": "Defines lifecycles of products, versions, and components nested within products.",
  "type": "object",
  "definitions": {
    "LifeCycle": {
      "type": "object",
      "description": "Represents a lifecycle, containing a list of products and defined lifecycle phases.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of this lifecycle."
        },
        "metadata": {
          "array": "array",
          "description": "A list of vendor specific metadata objects to the life cycle definition.",
	  "items": {
	     "type": "object"
	  }
	},
        "lifecycle_scopes": {
          "type": "array",
          "description": "A list of lifecycle scopes, and descriptions provided within the lifecycle.",
	  "items": {
            "$ref": "#/definitions/LifecycleScopes"
	  }
        },
        "lifecycle_phases": {
          "type": "array",
	  "description": "A list of lifecycle phase provided within the lifecycle.",
          "items": {
            "type": "string"
          },
          "uniqueItems": true
        },
        "products": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Product"
          }
        }
      },
      "required": [
        "name",
	"lifecycle_scopes",
        "lifecycle_phases",
        "products"
      ]
    },
    "LifecycleScopes": {
      "type": "object",
      "description": "Defines the specific support scopes provided by the lifecycle.",
      "properties": {
	"name": {
          "type": "string",
          "description": "Defines the name of the support scope."
        },
        "description": {
          "type": "string",
          "description": "A description of what the scope provides."
        },
        "metadata": {
          "array": "array",
          "description": "A list of vendor specific metadata objects to the product definition.",
	  "items": {
	     "type": "object"
	  }
	}
      },
      "required": [
        "name",
        "description"
      ]
    },
    "LifecyclePhase": {
      "type": "object",
      "description": "Represents a specific phase (or period) of a lifecycle phase within a version, referencing a defined lifecycle phase name.",
      "properties": {
        "name_ref": {
          "type": "string",
          "description": "References a name from the parent LifeCycle's lifecycle_phases array."
        },
	"support_scopes_ref": {
	  "type": "array",
	  "description": "What offering scope are included as part of this life cycle phase",
	  "items": {
	    "type": "string",
            "description": "References a name from the parent LifeCycle's lifecycle_scopes array."
	  }
	},
        "eox_lifecycle_mappings": {
          "type": "array",
          "description": "A mapping to an eox life cycle standard. The end_date from this product or the parent_referance is used to denote the end date for the mapping phase.",
	  "items": {
	     "type": "string",
	     "enum": [
		"end_of_maintenance (EOM)",
	        "end_of_security_support (EoSSec)",
	        "end_of_sales (EOS)",
		"end_of_life (EOL)",
		"end_of_technical_support (EoTS)"
	     ]
	  }
        },
        "metadata": {
          "array": "array",
          "description": "A list of vendor specific metadata objects to the product definition.",
	  "items": {
	     "type": "object"
	  }
	},
        "start_date": {
          "type": ["string", "null"],
          "format": "date",
          "description": "The start date of the phase (YYYY-MM-DD), or null if inherited or undefined (phase starts at the close of a prior phase)."
        },
        "end_date": {
          "type": ["string", "null"],
          "format": "date",
          "description": "The end date of the phase (YYYY-MM-DD), or null if inherited or ongoing."
        },
        "parent_reference": {
          "type": ["string", "null"],
          "description": "The name_ref of a lifecycle phase in the parent version to inherit dates from, or null if defined locally."
        }
      },
      "required": [
        "name_ref",
	"support_scopes_ref", 
        "eox_lifecycle_mappings"
      ],
      "anyOf": [
        {
          "required": [
            "start_date",
            "end_date"
          ]
        },
        {
          "properties": {
            "start_date": { "const": null },
            "end_date": { "const": null },
            "parent_reference": { "type": "string" }
          },
          "required": [
            "parent_reference"
          ]
        }
      ]
    },
    "Product": {
      "type": "object",
      "description": "Represents a product with its name, vendor, versions, and components.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "type": {
          "type": "string",
          "description": "A type defintion for the product, defining a classification for the product.",
	  "enum": [
            "software",
            "software_service",
	    "hardware",
	    "service"
	  ]
        },
        "product_acronyms": {
          "type": "array",
          "description": "A list of acronyms that represent the the product.",
          "items": {
      	    "type": "string"
	  }
    	},
        "prior_product_names": {
          "type": "array",
          "description": "A list of prior names that product has used or gone by.",
          "items": {
      	    "type": "string"
	  }
        },
        "vendor_name": {
          "type": "string",
          "description": "The name of the vendor for this product."
        },
        "versions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Version"
          },
          "description": "List of versions for this product."
        },
        "components": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Component"
          },
          "description": "List of components associated with this product."
        },
        "metadata": {
          "array": "array",
          "description": "A list of vendor specific metadata objects to the product definition.",
	  "items": {
	     "type": "object"
	  }
	}
      },
      "required": [
        "name",
        "vendor_name",
        "versions"
      ]
    },
    "Version": {
      "type": "object",
      "description": "Represents a specific version of a product, referencing lifecycle phases from the main schema.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name or identifier of the version."
        },
        "release_date": {
          "format": "date",
          "description": "The GA (general avalabilty) date of the version (YYYY-MM-DD)."
        },
        "weight": {
          "type": "number",
          "description": "A weight value for the version allowing for ordering when/if the version used doesn't follow symantic versioning."
        },
        "lifecycle_phases": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/LifecyclePhase"
          }
        },
        "metadata": {
          "array": "array",
          "description": "A list of vendor specific metadata objects to the product definition.",
	  "items": {
	     "type": "object"
	  }
	}
      },
      "required": [
        "name",
	"release_date",
        "lifecycle_phases"
      ]
    },
    "Component": {
      "type": "object",
      "description": "Represents a component, an included product or dependency that included/associated with a product.",
      "properties": {
        "product_name_ref": {
          "type": "string",
          "description": "References the product definition that defines this component as a product."
        },
        "product_version_ref": {
          "type": "string",
          "description": "References the product version definition that defines what version of a product is associated with the component."
        }
      },
      "required": [
        "product_name_ref",
        "product_version_ref"
      ]
    }
  },
  "properties": {
    "life_cycles": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/LifeCycle"
      }
    }
  },
  "required": [
    "life_cycles"
  ]
}
